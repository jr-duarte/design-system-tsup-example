import { styled } from '../stitches.config';

export const Wrapper = styled('div', {
  minHeight: '400px',
  width: '100%',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  padding: 5,
});
